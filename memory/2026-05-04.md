# Session Notes — May 4, 2026

## Galaxy Demo Game Updates

### Dog Pet Added
- **Assets:** Extracted 158 frames from 7 dog sprite sheets into `assets/iso/dog/`
  - idle_down/right/up (25 frames each)
  - walk_right (25 frames)
  - run_right/up (25 frames each)
  - run_down (8 frames from zip spritesheet)
- **Code changes in `src/game.js`:**
  - `loadDogSprites()` — loads all dog frames at boot
  - `createDogAnimations()` — builds idle, walk, run animations with frame subsets
  - `DogPet` class — follower pet that trails the player with slight random wander
  - Dog spawns automatically with player, updates in game loop

### Galaxy Casino Added
- **Room registry:** Added `casino: { name: 'Galaxy Casino', bg: 0x2a0a0a, next: ['town'] }`
- **Town:** Casino door at center (480, 260) between Coffee Shop and Dance Club
- **CasinoScene:** Full room with red carpet, 6 slot machines, poker table, neon lights, "GALAXY CASINO" marquee

### Warrior of Black Corrosion NPC Added
- **Asset:** `Mobile - Octopath Traveler_ Champions of the Continent - Playable Characters - Warrior of Black Corrosion.png`
- **Extraction:** 147 frames from 440×682 sprite sheet (10 cols × 16 rows, 44×44px grid) into `assets/iso/npc_warrior/`
- **Re-extraction fix:** Applied 2px inset crop to remove stray pixels from neighboring frames
- **Code changes:**
  - `loadWarriorSprites()` — loads all warrior frames
  - `createWarriorAnimations()` — idle, walk, run animations for 4 directions
  - `WarriorNPC` class — standalone NPC with wander, chat, red name tag

### Warrior Bug Fixes (iterative)
1. **Scale:** 2.5× → 1.2× → 2.0× → settled at **1.7×**
2. **Blurry sprites:** Added `pixelArt: true` to Phaser game config for crisp pixel-art scaling
3. **Idle animation bleed:** Fixed `stopMoving()` to use plain string key instead of config object with `repeat: -1`
4. **Wrong facing:** Removed per-frame facing recalculation during movement
5. **Behavior mismatch:** Matched `OtherPlayer` exactly — same speed, wander intervals, distances, chat timers

### Game Architecture Notes
- All rooms share `RoomScene` base class
- `BootScene` loads sprites + creates animations once
- `pixelArt: true` affects ALL sprites (nearest-neighbor filtering)

---
*Saved during pre-compaction flush*
