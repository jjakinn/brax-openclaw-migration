# 2026-05-09 — LPC Character System Implementation

## Godot App Setup
- Moved Godot from `/Users/Jakin/Downloads/Godot.app` → `/Applications/Godot.app`

## LPC Asset Audit
- Audited existing assets in `/Users/Jakin/galaxy-demo/assets/lpc/`: only ~15 sprite sheets (0.5% of full library)
- Full LPC library at `~/ClawMind/lpc-generator/spritesheets/` has 151,875 PNG files across 21 categories
- Full catalog has 655 options across 104 categories

## Palette Shader System
- **`shaders/palette_recolor.gdshader`** — CanvasItem shader that swaps 6 skin-tone colors via RGB distance matching (tolerance 0.02)
- **`scripts/PaletteManager.gd`** — Autoload singleton with 103 palettes:
  - 29 body tones (light through coffee)
  - 24 cloth colors
  - 32 hair colors
  - 8 metal colors
  - 10 eye colors
- **`scripts/LPCLayer.gd`** — Updated with `set_palette()` / `clear_palette()` that applies ShaderMaterial at runtime

## Asset Path Resolution
- **`assets/lpc_path_map.json`** — Generated from sheet_definitions using `tools/gen_path_map.py`
  - 641 items mapped across 104 categories
  - Each entry has: base_path, pattern (flat/colored), has_colors, is_bg, body_type, variants
  - Prioritizes "colored" pattern when color variants exist (walk/anim/color.png)
- **`scripts/LPCAssetResolver.gd`** — Updated to use path map first, with body-type filtering and fallback

## Character Creator UI
- Accordion sections: Body Type, Skin Tone, Head, Hair, Facial Hair, Torso, Legs, Feet, Accessories, Weapons, Shields
- Skin tone swatches — 22 color buttons using actual palette colors with selection highlight
- Body type thumbnails with toggle buttons
- Color variant swatches for clothing using palette colors
- Preview controls: Walk/Idle/Run, 4-directional arrows, zoom in/out
- Fixed accordion toggle signal connections (headers now properly expand/collapse)

## Layer Ordering (Z-Order)
- Added `LAYER_Z_ORDER` dictionary to CharacterCreator.gd, LPCAssetResolver.gd
- Back-to-front rendering: body → legs → feet → torso → head → hair → accessories → weapons/shields
- All three render paths (CharacterCreator, Player, OtherPlayer) now sort layers before adding

## Player / OtherPlayer Integration
- **`scripts/Player.gd`** — Auto-loads saved character on `_ready()`, supports runtime equip/unequip, broadcasts to network
- **`scripts/OtherPlayer.gd`** — Receives network character data, rebuilds LPC layers with z-order sorting
- **`scripts/LPCCharacterData.gd`** — v2 save format with skin_tone, network dict support
- **`scripts/MultiplayerManager.gd`** — RPC sync for character appearance

## Key Bugs Fixed
1. **Parse error** in CharacterCreator.gd — orphaned dead code + tab/space mismatch
2. **Preview not updating** — `_update_preview()` preserved "idle" animation but most items only have "walk" frames; fixed to always play "walk"
3. **Palette applied to wrong layers** — Only body layers get skin tone shader; clothes/hair/weapons use pre-colored PNGs
4. **Preview off-screen** — Centered at Vector2(250, 320) under "Preview" label
5. **Character facing away** — Fixed to direction 0 (south/down, facing viewer)
6. **Missing head on startup** — `_ensure_default_body()` now also adds default "Head Color" layer
7. **Head/hair layering wrong** — Added z-order sorting so hair (z=90) always renders on top of head (z=60)

## Files Modified
- `scripts/CharacterCreator.gd`
- `scripts/LPCLayer.gd`
- `scripts/PaletteManager.gd` (new)
- `scripts/LPCAssetResolver.gd`
- `scripts/Player.gd`
- `scripts/OtherPlayer.gd`
- `scripts/LPCCharacterData.gd`
- `shaders/palette_recolor.gdshader` (new)
- `assets/lpc_path_map.json` (new, generated)
- `project.godot` (PaletteManager autoload)

## Tools Created
- `tools/gen_path_map.py` — Generates lpc_path_map.json from sheet_definitions
