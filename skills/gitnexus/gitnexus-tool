#!/bin/bash
# GitNexus Skill Tool Wrapper
# Usage: gitnexus-tool <action> [options]

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON="${PYTHON:-python3}"

# Run the Python bridge
exec "$PYTHON" "$SCRIPT_DIR/gitnexus_bridge.py" "$@"
